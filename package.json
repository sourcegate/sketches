{
  "scripts": {
    "start": "live-server & open /Applications/Simulator.app && sleep 5 && xcrun simctl openurl booted http://localhost:8080",
    "predeploy": "node generate-directory-listing.js",
    "build": "npm run predeploy"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "@radix-ui/react-icons": "^1.3.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "live-server": "^1.2.2",
    "tailwind-merge": "^1.14.0",
    "tailwindcss-animate": "^1.0.6"
  },
  "bundleDependencies": [
    "tailwindcss"
  ]
}
